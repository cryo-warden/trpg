"Phase 1 landed (commit 8141b96): name-canonical STRUCTURE. TS Records keyed by name for every asset kind (dup names = TS error; keyof literal name types); wire AssetPack = author forms with name-only cross-refs (steps nested in actions, connections nested in maps); push_assets is the sole forward name->id converter (interns per kind in enumeration order, Err on dup/unknown); asset tables keep int PKs + unique name column (added to appearance_features/encounter_blobs/encounters/location_map_themes). Deliberate remainders, commented in code: (a) blob-internal asset refs (baseline/traits/hotkeys/appearance) still ids on wire, client-converted by matching enumeration -- needs asset-name selectors in blob fields; (b) client display still indexes ordered Record views -- must switch to subscribed-table id->name lookup WHEN phase 2 lands. PHASE 2 (this task's remaining core): incremental re-push semantics -- match incoming names to existing rows, preserve their ids, add new names with fresh ids, update changed bodies, define deletion policy; then the client backward conversion via subscribed tables becomes mandatory. Also fixed latent test fragility: hardcoded appearance indexes (dup 'path' feature removed shifted them). Verified: cargo test, clippy 0 errors, web check + 101 unit tests, e2e 0-5 all green."