"Implemented the selector layer (commit 6a0ecbf): entity! generates required registry table (name unique -> entity_id), EntityIdSelector (Literal|Local|Named), InstantiationScope { ecs, locals }; EntityId-typed blob fields become selectors via type-based detection; blob-consuming fns take scope + return Result; _row/_new fns stay infallible for live-handle mutation; EntityHandle::register_name via try_insert. new_player's magic from_name(allegiance1) eliminated -- allegiance ships in the new-player blob as Named; named blobs pushed via AssetPack.named_instantiate_entity_blobs (instantiated then registered, before anonymous blobs so they can be referenced). from_name removed. Verified: cargo test, clippy (0 errors), web check + 103 unit tests, e2e 0-5 incl. NEW assertion that the player's allegiance id equals the named_entities row for allegiance1. Remaining for this task's full scope: intra-batch Local refs are implemented+compile-tested but no production use yet (container/inventory case will exercise them); Named currently resolves entities only -- asset-kind names covered by name-canonical task."