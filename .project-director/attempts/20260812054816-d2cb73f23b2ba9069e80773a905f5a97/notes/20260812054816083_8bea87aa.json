"Implemented (commit d381d7e) per approved design. entity macro: component declarations accept dirties(flag_component, ...) alongside the name-in-table pairs; targets resolve against declared components at macro expansion (unknown name = compile error); every generated EntityHandle mutation path (blob insert/update, _row, _new, delete) auto-inserts the declared flags if absent. Applied in entity.rs: baseline dirties total_stat_block_dirty_flag; traits dirties traits_stat_block_dirty_flag; all StatBlockCacheComponent instances dirty total_stat_block_dirty_flag. Consequences: instantiate_blob_dirty extension DELETED (plain instantiate_blob dirties via its component writes); entity_stats_system's manual total-flag upsert removed (cache upsert auto-dirties). Verified live: mapgen e2e player stats materialize purely through auto-derived flags; full gate green. ALSO this turn: Author->Asset rename (f52eb6a): wire types follow the ...Asset convention (asset/types.rs), TS assets ARE the generated types (sugar layer deleted, ~300 lines net removed; namedPairs bundling is the only client step; ACTION_APPEARANCES split out as client-only display vocabulary)."