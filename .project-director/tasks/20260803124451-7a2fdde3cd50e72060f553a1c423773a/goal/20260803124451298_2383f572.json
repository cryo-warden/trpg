"The GM/testing operations from the orphaned server/src/admin.rs -- live damage (admin_damage), add trait (admin_add_trait), create/recreate a player with a chosen allegiance, starting room, baseline, and traits (create_player_for_self / admin_recreate_player_for_identity), and apply-bundle -- are genuinely useful both for GM operations and for testing. User's note: the proper place for these is NOT amalgamated into a single admin file; they belong in a dedicated admin reducers module (at least, e.g. server/src/reducers/admin.rs). Reimplement them there, wire the module in (currently orphaned; not in any mod, references stale crate paths), gate them behind the published-secret-token auth, and delete the old admin.rs once migrated. Also carries a real technical question from admin.rs: whether permission checks can run before reducer argument parsing/deserialization."