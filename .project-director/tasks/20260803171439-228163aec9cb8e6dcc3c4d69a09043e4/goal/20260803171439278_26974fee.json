"Re-include web-trpg *.test.ts(x) in tsc so the check phase type-checks the tests (they currently run/type-strip under bun test but are excluded from tsc via tsconfig.app.json). Blocker to solve: with the files re-included, tsc reports TS2307 'Cannot find module bun:test' for every test even though @types/bun is installed -- @types/bun's bun:test module declaration is not picked up under moduleResolution=bundler. Investigate the fix (e.g. compilerOptions.types including 'bun', a /// <reference types=bun />, a bun-types entry, or a dedicated tsconfig for tests) without breaking app type-checking, then drop the exclude in tsconfig.app.json. Also ensures the mock-connection casts in components.test.tsx type-check."