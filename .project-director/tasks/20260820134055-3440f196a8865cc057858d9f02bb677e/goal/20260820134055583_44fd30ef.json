"Define the four group blocks as independent structs, each carrying only its own fields and its own operations (accept parallel add/negate repetition between numeric groups rather than reabstracting): StatsBlock (mhp/mep/attack/defense/size), AppearanceBlock (feature id-vec, union add), BodyCapacityBlock (hand/body/relic; owns admits_equipment_item, reads only these three), ReadinessBlock (morale + bladed/blunt/pole/ward/focus/gait/reach/wing/upright/fang/claw/ooze + room for further action-suggesting tags; owns meets/ReadinessRequirements for action gating). Define the matching per-group ASSET types (authored, name-resolving where needed). Introduce ADDITIVELY alongside the existing StatBlock so this lands green (clippy/generate/rust-tests) as a checkpoint; unit-test each group's ops. No pipeline/source/asset-authoring changes yet; StatBlock not yet deleted."