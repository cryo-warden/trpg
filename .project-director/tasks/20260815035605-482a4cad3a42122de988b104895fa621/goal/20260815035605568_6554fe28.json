"The equip menu shares the stance card's exact shape (categorized detailed stats, armament buttons, action bar) plus armor/relics; its values are the DEFAULT configuration (no stance) so NO deltas render - it is the base stances compare to; no view shows (0) anywhere. New DEFAULT action bar mirrors the default armament slot: set_default_actions reducer (geared stance-free pool), adoption pins it when the stance has no bar, active-stance None falls back live, stance cards gain the same use-default toggle."