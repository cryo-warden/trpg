"Teach ecs-macro a #[join(...)] declaration (two indexed entity columns + payload, optional dirties()) generating the accessor and both-sides cleanup inside delete_entity_with_joins, so genuinely relational per-entity state (entities_visited_locations stays a join for its reverse index) gets the macro's lifecycle guarantees instead of hand-wired cleanup hooks — the forget-to-hook bug class the macro exists to kill."