"An entity deleted in the same transaction as its final event (an eaten cookie) must still render by its last-known look, never as 'something'. The client keeps a RETAINED map of appearance data under the live rows: every insert/update refreshes it, deletion retains, and name resolution falls back to it when the live row is gone. Presentation data only - no gameplay reads the cache."