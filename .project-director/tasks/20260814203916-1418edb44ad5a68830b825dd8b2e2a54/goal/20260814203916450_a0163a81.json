"Generated map instances are private: reused only by the owning player (later, their party). An instance marks its player; paths route different players to different destination instances. Exception: designated COMMON ZONES (towns) are shared, instancing anew only when existing instances exceed ~1 player per room. Party membership side-steps all instancing rules: you always join your party members. Party = abstract entity, a unique pointer for player party components."