"ANSWER RECORDED (2026-08-12): cross-map connections are NOT implemented -- LocationMapConnection rows (exit map -> destination map, from connectionNames) are stored and rebuilt per push but map generation never consumes them; no inter-map paths exist. USER DESIGN superseding that table: a join table specifying DIRECTION (including both-ways) and HOW to connect on each side -- anchor kinds like a branched room, an edge room, the ending, the entrance, maybe more. Implementation implies: generation resolving both maps' anchor rooms and instantiating themed path entities between them (cross-instance timing: maps generate lazily per player today -- connection generation must handle the far map not yet existing), plus replacing connection_names authoring with the richer directed shape."