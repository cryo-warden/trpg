"Extend client test coverage through the DI seam established in StdbContext (ReadableTable selectors + an in-memory mock connection injected via StdbContext.Provider; first test is useLocationEntities in components.test.tsx). Work: (1) extend the mock connection with unique-index find (table.entityId.find / table.identity.find) so component-level hooks like useHpComponent, usePlayerEntity, and useActionOptions can be tested; (2) handle bun's .css imports (a loader/stub) so full presentational components (HPBar, EPBar, EntityName, panels) can render under happy-dom; (3) extract the inline mock connection + wrapper from components.test.tsx into a reusable test-support helper; (4) add tests for reactive rendering paths -- player-see-room prominence ordering, vitals bars, action options -- asserting rendered text/DOM. Keep these in the cheap client unit phase (test-web)."