"Mirror the split on the client: subscribe to the per-group total/source components (replacing the single equipment/status stat-cache subscriptions); split web-trpg statBlock arithmetic into the four independent group operations (no composition type). Menu config-driven projection reads body-capacity (apply-if-fits) plus the rendered stats/readiness. READINESS UI RULE: show only the readiness stats that are non-zero on ANY of the entity's stat SOURCES — even when the final total nets to zero, a value that moved at all is relevant (needs per-source readiness visibility, or a server-published set of readiness keys touched by any source). Mirror the derived action set (readiness candidates gated by active customization). Update the mock connection, component hooks, and CustomizationPanel/StancesMenu tests. test-web green."