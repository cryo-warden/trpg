"Cut the whole runtime over to the group blocks and DELETE StatBlock/StatBlockAsset and every reference. Author assets per group: each source (baseline/trait/stance/quest/gear) carries per-group Option data; each entity type authors only its relevant groups (physical object = stats + appearance; player = all). Give each group its own total component + dirty flag. A source change dirties ONLY the groups where its Option is Some (sources are the sole mixing layer); per-group total systems recompute and value-diff to skip unchanged downstream. Remove apply_stat_block: the per-group totals ARE the canonical applied/read state; downstream combat reads the stats total directly, retiring redundant per-stat components where clean, keeping only genuinely-runtime mutable state (current hp/ep + accumulated deltas) and the mhp/mep ratchet. Equipment computation is dirtied ONLY by body-capacity changes and still drops items that violate capacity (apply-if-fits, unchanged). morale lives in readiness; appearance is universal on physical objects. Rust + seam (test-unit) green."