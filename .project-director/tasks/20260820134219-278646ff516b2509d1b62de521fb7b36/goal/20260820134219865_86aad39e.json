"Replace stored/granted action_ids with a fully DERIVED action set. Candidates = every action whose requirements are met by the entity's READINESS. A customization configuration selects which candidates are active; an action is valid while its customization is active AND passes a readiness re-check at selection/performance validation (readiness may have dropped, e.g. fear lowering morale). Remove action_ids from the source/grant model and from stat storage entirely. Update action validation, the action bar/available-action derivation, and the customization/loadout flow accordingly."