"End-to-end verification after the split. Existing equipment/stance/status/customization flows stay green. Assert the isolation wins: a non-capacity source change (fear/courage/braced, a readiness- or stats-only edit) never re-derives equipment computation (only body-capacity dirties it), yet capacity-violating items still disable. Assert the readiness UI shows readiness keys that moved on any source even when the total nets to zero. Assert the derived action set: readiness-permitted candidates, valid only while their customization is active and re-checked at use. Full test-e2e + test-e2e-customization exit 0."