"Implemented (2026-08-08). ecs-macro: new gen_struct/component_blob_struct.rs generates <Component>Blob per component declaration (declared fields only, column attrs primary_key/unique/index/auto_inc stripped, derives SpacetimeType + struct_attrs); gen_impl/component_blob_struct_impl.rs generates From<Component> for Blob and Blob::into_component(id) (replaces deleted component_struct_impl.rs, i.e. Component::new with sentinel id 0 is GONE). EntityBlob fields now Option<ComponentBlob>. Handle mutation API (insert_/update_/upsert_ + _new variants) takes blobs; EntityHandle impl injects self id via into_component; WithComponent zero-arg update converts held component via Into. new_blob maps components into blobs. Server: entity_handle_extension.rs converted (update_hp(x.into()) etc., ActionStateComponentBlob literal, queued->action move via .into()). ecs test sandbox exercises blob round-trip + instantiate_blob. TS: entityId: 0n removed from entity_blobs.ts/encounters.ts/e2e testAssets.ts. VERIFIED: cargo test all green (ecs + server 9 tests). REMAINING: bun run stdb-generate (bindings regen), bun check/lint/ts-check, bun test src, cargo clippy --all-targets, e2e bundle -- blocked on session permissions (only cargo test granted; po run blocked by PROCESS_ORG_SANDBOX_MOUNT unset). Commit deferred until full verify."