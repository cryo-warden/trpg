"PARTIAL: cleanup TIER delivered — map teardown now uses a silent cleanup_delete (no spill/debris/events); 3-tier defeat (death/destruction/cleanup) is in place. REMAINING (deferred): converting the imperative recursive teardown into the pure global-invariant form (room_orphan/location_orphan/path_orphan). location_orphan would delete non-player content the fake-Literal-room e2e packs (combat/stances/loadout/morale/graph) rely on, so per the 'B' decision it needs those full-tick e2e tests migrated to seam-based subset tests first. Map lifecycle otherwise functionally complete."