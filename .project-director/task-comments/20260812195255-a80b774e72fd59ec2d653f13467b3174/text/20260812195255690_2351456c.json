"Efficiency escape hatch, deferred (2026-08-12, user): if stat-block growth ever matters after performance testing, split StatBlock into separate LOGICAL STAT SETS (e.g. combat stats vs anatomy/slot properties vs size/navigation), each an optional block on the assets that carry it. Reduces copies and summation work to only the sets an asset actually contributes; the cost is optionality (Option<SetBlock> per set) on asset shapes and merge code per set. Not to be done preemptively -- single flat StatBlock stands until measured."