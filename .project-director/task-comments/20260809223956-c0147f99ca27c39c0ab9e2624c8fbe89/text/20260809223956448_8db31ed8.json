"Wrap-up (2026-08-09): e2e combat phase now resolves its action id by name through the subscribed actions table (last enumeration-order assumption in tests removed; ATTACK_ACTION_ID constant deleted). NOTE: the web client's init() still pushes on every connect -- per user direction this is now WRONG (clients cannot push automatically; only an admin may). Its removal is item (6) of the spec on admin-auth-via-a-published-secret-token (value 5, the high-priority next task, story gm-admin-gated-asset-pushes). Accepted as a known interim until that lands."