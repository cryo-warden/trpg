"CORRECTION (2026-08-09, user): name resolution WILL be built into the macro -- the previous 'macro must stay name-blind, inject a resolver' framing is superseded. Names stop being a component (NameComponent dies) and become a NATIVE part of defining assets, required for asset migrations where an asset moves to a different index (index-based cross-references break; name is the canonical stable identity). For entity references this means the selector's external variant is Named(name) and the macro itself owns resolution, because the macro also natively owns the name->entity mapping for instantiated named blobs (not an app-level lookup). Proposed concrete shape pending confirmation: macro-generated named-entity registry table (unique name -> entity_id) written when a blob is instantiated under a name; selector = Literal(EntityId) | Local(u32) | Named(String); resolution reads the registry via the ecs context; Result on failure. Open points sent to user: (a) does the blob declaration itself carry the name (blob asset name = registry key), or is the name supplied at instantiation time; (b) is the entity-name namespace global or per asset kind; (c) param name for the instantiation context ('resolver' proposal still pending)."