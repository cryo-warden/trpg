"PHASE 1 LANDED (2026-08-12). Server: StatBlock gains property stats hand/gait/reach (i8 section); StatRequirements stamped Option-threshold struct + StatBlock::meets() (3 unit tests); Action rows/assets carry requirements; Stance asset kind (stances table, public; requirements + stat_block) in the full name-canonical push pipeline w/ <=6 granted-actions cap; ActiveStanceComponent dirties(total_stat_block_dirty_flag); EntityBlobAsset.stance_name; entity_stats_system: base_stat_block() helper (baseline + traits/equipment/status caches -- placeholder caches now actually merged) + stance contribution + requirement-filtered derived ActionsComponent; set_stance reducer validates stance requirements against the stance-free base (instant swap; round cost comes with loadouts). Client: stances + active_stance_components subscribed; useStanceRows/useMyActiveStanceId; StancePanel (own grid area beside pinned bar) w/ active highlight + swap; assets: stat_block.ts + stat_requirements.ts helpers, stances.ts (brawler requires hand>=1; amorphous; prone gait-2/atk-1/def-1), baselines provide hand/gait/reach (human 2/2/1, slime gait 1, bat gait 2), move/quick_move require gait>=1, bop/boppity_bop hand>=1; new_player blob stance brawler; mocks extended; 2 StancePanel unit tests. e2e phase 7-stances (5 tests): derived actions = baseline grant requirement-filtered; blob-authored stance present; prone swap drops the movement action; swap back restores; hand>=4 stance rejected. Registered test-e2e-stances in .process-org.json bundle. Full gate green: cargo test (13), clippy 0 errors, bun check, 101 unit tests, e2e phases 0-7. NEXT PHASES (recorded in plan comment): armament assets + equipment properties + loadout prep tables + preview + one-round swap action; forced-stance contests (size deltas) + authored exits; relations/statuses; quest-driven acquisition + slot menus."