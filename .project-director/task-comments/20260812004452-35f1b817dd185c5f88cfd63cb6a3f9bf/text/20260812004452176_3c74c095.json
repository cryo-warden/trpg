"Decisions (2026-08-11, user): (1) PROMINENCE LEAVES THE SERVER ENTIRELY -- remove EntityProminenceComponent, generate_prominence, the table, and the blob field; presentation ranking is pure client concern computed from subscribed component state. (2) The client renders TWO VIEWS of the one system: a threat-oriented view when the threat predicate holds, an exploration-oriented view otherwise. Path placement differs: exploration view gives PATHS their own section at the TOP; threat view mixes paths into the same section as decorations (demoted surroundings). (3) DESIGN NOTE (considered, not committed): a room-description section above the current messages view -- needs template upgrades and a new way to assign description templates to rooms -- where decoration names appear as clickable inline text instead of entity-level emphasis; if built, paths ALSO render there as buttons under the description; the client records the entity's last-used movement actions, and clicking a path button immediately reuses the most recent movement action that is valid for that path (fast travel input without new server semantics -- act() unchanged)."