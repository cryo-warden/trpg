"Delivered in 346c1a1: LocationMap.quest_spawns (quest_id + item blob + guaranteed/eligible indexes + half-open eligible count); push validates indexes vs bit_count and refuses a bit guaranteed by two maps; quest.rs apply_quest_spawns consumes the role-tagged MapGenerationResult via map_materialization::materialize_map. Production red/blue cookie windows overlap across all 7 maps; e2e phase 15 covers the pipeline."