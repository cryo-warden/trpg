"Delivered incl. mid-flight refinements: (1) armament buttons queue registered equip/unequip acts against the exact item entity; (2) visible disablement (Button :disabled style) for overweight armaments, capped relics, unregistered verbs - live re-evaluation; (3) grip basis corrected per user note: the DEFAULT configuration's hand (total minus in-hand set plus default set), fixed on BOTH sides - server equip_item + take auto-wield now use default_configuration_hand, menu mirrors it (stances menu already used the shown stance's candidate); (4) owned-item lists sort by entity id at the source (useOwnedItems + useTargetIsEquipped) fixing the click-one-light-another instance bug; (5) stances-menu 'use default' toggles active->Some([]) custom-empty; (6) clicking an active-by-default item copy-on-writes defaults-minus-clicked (was correct once identity stabilized; pinned by test); (7) turn-pause pill moved into the events grid area - centered over the message panel, never over menu stats. Principle recorded: menu-visibility-implies-modifiability."