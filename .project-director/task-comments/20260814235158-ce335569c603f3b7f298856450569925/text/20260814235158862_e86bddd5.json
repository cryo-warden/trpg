"Refined per user direction (anticipation): direct set_quest_bit on defeat REPLACED by defeat drops. QuestRoomClaimAsset.defeat_drop {questName, index, itemBlob} - drop names its OWN quest, so cross-quest holds (one quest's monster carrying another quest's item) are supported by the same shape; old_keep uses the self-referential form. drop_defeat_reward: last-living-carrier + instance scoping unchanged; spawns max(1, players present) items into the room with the QuestItem ref stamped, riding hidden-cookie logic end to end (take, eat, freshness, quest cache). Blob resolved at death via instance->map->claim (EntityBlob cannot nest in a component - recursion). e2e phase 22 now walks the full loop: kill -> exactly 1 drop on floor, bit NOT yet set -> take -> eat -> bit + mhp 12, other instance's warden untouched."