"STANCE CHANGES ARE ACTIONS -- LANDED (2026-08-13). The user-directed model: 'stand', 'sit', 'lie down' etc. are ordinary round-costing actions; the StancePanel bar is GONE (grid returns to a full-width pinned bar; the stats view shows the current stance by name). Server: ActionEffect::SetStance(stance id) + ActionType::Posture (self-target only); AUTHORED effects split from stored effects -- new ActionEffectAsset enum (SetStance carries a stance NAME) with resolve_action_effect() at push, and AssetNameMaps now builds before the action loop so effect payloads can reference ANY kind (the seam future effects like summons will use). One adoption path: try_adopt_stance() on EntityHandleExtension (fear gate, known gate, requirements gate, status shedding, loadout re-arm) shared by the set_stance reducer (kept as the dev/menu path) and the SetStance effect (a refused adoption fizzles the round with a debug log). Content: 14 posture actions (stand/sit/lie_down/ready_up/square_up/duel/stride/perch/take_wing/center/kindle/chill/charge/slump) granted by the same bodies that grant the stances; admin trait grants all; appearances use the possessive rule. e2e: phase 7 gains the round-costing posture-action test (act test_lie -> prone -> grants re-derive) and its exact-set assertions updated; full ladder 0-12 green, 106 unit tests (StancePanel tests removed with the panel), clippy 0; dev reseeded."