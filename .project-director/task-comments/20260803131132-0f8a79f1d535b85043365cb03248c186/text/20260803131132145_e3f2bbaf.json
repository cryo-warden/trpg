"Strategy decision (2026-08-03): do NOT build a backend stdb mock / DI seam -- too expensive. Test in the real engine (Factorio-style). Rely on multi-phase gating so cheaper phases short-circuit before expensive ones: cheapest unit tests -> cheaper integration tests -> expensive integration tests. The po verify bundle enforces the ordering (dispatch bundle stops at first failure)."