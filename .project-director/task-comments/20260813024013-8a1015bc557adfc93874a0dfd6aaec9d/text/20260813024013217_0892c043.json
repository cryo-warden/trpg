"SETUP ADVANCED via two elevated runs (2026-08-13). Discovery: sources root = /home/keen/deployments/sources; the unrestricted cw CLI has NO register/deploy/env verbs (HTTP API or web UI only) and no CRYO_WARDEN token exists in the shell env -- so registration is deliberately left to the operator's UI (secrets never pass through the agent). DONE: bare fetch-only clone created at /home/keen/deployments/sources/trpg (origin = the working repo; each deploy fetches current master), confirmed listed beside the other services. REMAINING (operator, web UI): one-click register trpg from the dashboard (reads the committed validated manifest, auto-creates env placeholders); set values for TRPG_STDB_PORT (suggested 3010), TRPG_WEB_PORT, TRPG_STDB_DATA_DIR (suggest /home/keen/deployments/data/trpg-stdb), TRPG_ADMIN_TOKEN (fresh secret, provisional admin password), CARGO_TARGET_DIR (suggest /home/keen/deployments/data/trpg-cargo); deploy master. First-deploy watchpoints unchanged (spacetime start flags, PATH in cw spawn env, WS passthrough); agent can read aj cw deploy-log trpg and iterate on failures."