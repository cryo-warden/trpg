"E2E harness built (web-trpg/e2e/). Phases working against a real local SpacetimeDB instance, isolated per run (fresh publish to trpg-e2e --delete-data), prereq-gated, ordered smallest-first with stop-at-first-failure via the po bundle test-e2e -> {smoke, assets, world, map}, wired as the last phase of verify. Phase 0 smoke (publish + sql), 1 assets (SDK connect + init/pushAssets), 2 world (new_player -> stats + placement), 3 map (room/path graph generated). BLOCKED next: combat/player-attack E2E needs a player and an enemy co-located, but the default spawn map (start_zone) has no encounters and there is no reducer to place a player in a chosen room or spawn/damage on demand -- so it depends on the dedicated admin reducers (create-player-in-room, admin_damage). Revisit combat + deterministic topology phases after that task."