"SINGLE-PATH RESTRUCTURE + PLAYTEST DIRECTIVES (2026-08-13, target e2687d5): (1) ONE provisioning path (user): create_account/provision_account create ACCOUNTS only; player entities come exclusively from the new player_provision_system, triggered purely by 'account exists, player does not, new-player blob available' -- no connect hook, no per-flavor linkage; client_connected merely tolerates playerless accounts. Bootstrapped/created/provisioned accounts all converge. (2) Default player is a REASONABLE HUMAN: the admin trait (grant-everything) is off NEW_PLAYER_BLOB (mobile+bopper+sword remain); admin stays an account ROLE. (3) CORPSES: dead NPCs are never deleted -- controller removed (stops acting, stops reading as threat via canAct) but the entity, name, and 0-hp body persist until map cleanup sweeps the room. (4) BAKED NARRATION (user): the events panel renders each event exactly once at arrival and stores the node -- later death/despawn/rename can never corrupt an earlier message. (5) e2e hardening the universal provisioning demanded: playerEntityIdFor(account name) helper replaces every 'controller row [0]' selection (phases 4,7,9-13); phase 13 made instance-agnostic after a CONCRETE multi-instance ambiguity repro (the admin's own near-instance could receive the return path) -- the instance-ownership design question now has teeth. Full ladder 0-13 green; dev reseeded."