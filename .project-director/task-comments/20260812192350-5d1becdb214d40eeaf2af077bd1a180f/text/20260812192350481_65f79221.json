"Refinement (2026-08-12, user): DUAL WIELDING is just a counted property check -- requirements are over a MULTISET of property instances, not a set. Presence = count >= 1; dual wield = bladed >= 2; monk = free_hand >= 2; mixed stances compose (bladed >= 1 AND free_hand >= 1 for a duelist with an open off-hand). Deliberate semantic: requirements count property INSTANCES, not items -- each armament asserts its properties with authored multiplicity, so a twin-bladed staff asserting bladed x2 CAN satisfy a two-blade stance, which is a feature (authored per armament), not a loophole. ADJACENT CONSEQUENCE (hand economy): free_hand instances must be derived, not asserted independently -- the body PROVIDES grip slots (bipedal: hand x2), each armament CONSUMES some (two-handed spear: 2, sword: 1), and free_hand count = remaining slots. Armament-set internal validity (total consumption <= provided slots) is one more preparation-time check in the same system. This provides/consumes split is the resource half of the property model and also covers future shapes (four-armed bodies, shield straps that hold without a grip)."