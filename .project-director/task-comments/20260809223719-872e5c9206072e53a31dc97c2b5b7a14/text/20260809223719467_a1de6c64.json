"Spec update (2026-08-09, user; supersedes conflicting parts of the original goal): clients must NOT push assets automatically -- only an admin identity is trusted to push. Required pieces: (1) a roles table (explicit data model, no sentinel/flag), (2) an identity-roles relation mapping identities to roles, (3) a claim_admin_role reducer that requires the client to supply a secret token and grants the admin role to the calling identity (so the token bootstraps the ROLE; later privileged reducers check the role relation, not the token), (4) the publish action/flow updated to include the secret token for admin validation at publish time, (5) push_assets (and GM reducers) require the admin role, (6) the web client's init() stops pushing on every connect -- pushing becomes an explicit admin action. This is the HIGH-PRIORITY NEXT task. E2E note: the e2e harness must claim the admin role in its seeding clients."