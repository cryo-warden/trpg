"Scope correction (2026-08-09, user): naming is not merely 'per-kind named asset tables at the app layer' -- name resolution is built INTO the macro layer as a native property of asset definition. Motivation: asset migrations. Assets are today cross-referenced by numeric index (TS findIndex -> baselineId/actionId/...); when an asset moves to a different index those references break. Name becomes the canonical, migration-stable identity that references resolve through. NameComponent is eliminated; entity naming happens where the asset/blob is defined or instantiated, and the macro owns the name->id mapping."