"Defeat-bits delivered (follow-up commit): QuestRoomClaim.defeat_bit_index (Option, push-validated vs bitCount) stamps DefeatBitComponent{quest_id,index} on every entity the claimed encounter spawns (Encounter::populate now returns spawned ids). death_system calls quest::grant_defeat_bit: component consumed as one-shot latch (corpses linger); grant waits for last living carrier of the pair IN THE SAME INSTANCE (map_instance_id_of), then set_quest_bit for every co-located player. e2e phase 22: bit granted while the admin instance's warden still stands (instance scoping proven), +2 mhp lands via the quest cache. Remaining from goal: completion-gated doors."