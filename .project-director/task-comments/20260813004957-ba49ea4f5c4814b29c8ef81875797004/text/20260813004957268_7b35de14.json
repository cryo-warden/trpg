"REFINEMENTS LANDED (2026-08-12, user): (1) DEATH-TRANCE TIMER: dying no longer teleports instantly -- death_system sets a respawn timer (RESPAWN_TRANCE_MICROS = 3s; TimerComponent gains a respawn_timer pair) during which act and set_stance refuse ('The death-trance holds'); respawn_system fires the wake. (2) ABSTRACT CHECKPOINTS: CheckpointComponent is now (location_map asset id, checkpoint_index) -- NEVER a room entity, since the destination map may not be generated. New components: MapInstanceComponent (which asset a generated map realizes) + MapCheckpointsComponent (ordered generated checkpoint rooms) on the map entity; CheckpointBindingComponent on checkpoint OBJECTS (generation stamps its own map+index; blobs author bindings by MAP NAME via CheckpointBindingAsset -- location_maps now matched early in AssetNameMaps). Attune copies the object's binding. resolve_checkpoint_room(): find an existing instance of the map or GENERATE IT ON DEMAND, then index into its checkpoints -- explicitly the same resolution teleportation will use. New-player auto-checkpoint = (starting map, 0). OPEN QUESTION recorded: with multiple generated instances of one map (maps generate per player today), resolution picks the first instance found -- instance ownership/identity design pending. e2e 12 reworked: the haven map is never pre-generated; the seeded dice bind abstractly; the test asserts trance rejection while dead and a wake in a brand-new on-demand-generated room. Ladder 0-12 green, 108 unit, clippy 0; dev reseeded."