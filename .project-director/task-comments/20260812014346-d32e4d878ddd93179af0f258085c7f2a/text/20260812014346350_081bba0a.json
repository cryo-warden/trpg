"FIRST SLICE LANDED (commit 68343ec), all client-side as planned: (1) target->focus rename complete: FocusContext/FocusProvider/FocusPanel, focus never fires, ActionButton binds target = explicit prop ?? focus (the single-target-binds-focus rule); old Target* files stubbed pending deletion (tooling cannot rm; ask user or extend aj). (2) domain/threat.ts: hostiles = co-located hp-bearing non-allies via the SAME isAlly rule as attack validity (invariant: threat == attackable-non-ally, cannot drift). (3) Two views in the location panel: threatened -> Threats section leads, everything else incl. paths demotes to Surroundings; calm -> Paths section on top, then Here. Demote-never-remove honored. (4) Lone-hostile auto-focus DERIVED in FocusProvider (pure derivation, no stored state, explicit focus always wins) -- combined with existing hotkey->button machinery and the focus panel's hotkeyed action bar, the single-enemy attack is one keypress; no new input machinery was needed. Unit coverage: threat domain, auto-focus/override, focus clamp, panels. Full gate green. REMAINING SLICES: threat-view section styling polish; room-description panel (if committed); breakable decorations; Activate interactions; multi-target slot-declared actions + act() schema."