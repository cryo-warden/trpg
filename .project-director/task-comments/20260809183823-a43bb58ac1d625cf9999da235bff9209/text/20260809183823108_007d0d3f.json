"Correction landed (commit 81c743d), removing BOTH phase-1 remainders per user direction: (1) entity blobs now cross the wire as EntityBlobAuthor with name-based asset refs (baselineName/traitNames/actionNames/actionHotkeys.actionName/appearanceFeatureNames); push_assets resolves them via AssetNameMaps; client getEntityBlob deleted -- the client computes NO ids anywhere. Runtime-state components deliberately absent from the author form. (2) backward conversion is table-based NOW, not deferred: appearance_features made public; client subscribes actions + appearance_features; assetLookup.ts hooks map runtime id -> table row name -> local Record. Display refactors: language plugins are factories with injected actionAssetOf (framework-free preserved); appearance domain takes resolved feature assets, not indexes; getActionOptions takes injected actionAssetOf. Mock connection provides default asset tables mirroring a pushed Record (tests play the server role). Remaining task core (phase 2) unchanged: incremental re-push semantics (match names, preserve ids, update bodies, deletion policy -- user leaned keep-but-log). Verified: cargo test + clippy clean, web check + 101 tests, e2e 0-5 green."