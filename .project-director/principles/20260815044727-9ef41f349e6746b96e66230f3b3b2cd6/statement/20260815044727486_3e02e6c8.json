"Cross-cutting consistency rules are imposed by SYSTEMS observing state, never by special-casing every mutator: when a derived or dependent state must track a source (equipment tracking configuration, stat caches tracking components, turn flags tracking action states), one per-tick system detects divergence and drives convergence. Mutators then stay simple writes to their own state - new mutation paths get the rule for free, and the rule cannot drift between call sites."