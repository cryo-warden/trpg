// THIS FILE IS AUTOMATICALLY GENERATED BY SPACETIMEDB. EDITS TO THIS FILE
// WILL NOT BE SAVED. MODIFY TABLES IN YOUR MODULE SOURCE CODE INSTEAD.

/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
import {
  AlgebraicType,
  AlgebraicValue,
  BinaryReader,
  BinaryWriter,
  CallReducerFlags,
  ConnectionId,
  DbConnectionBuilder,
  DbConnectionImpl,
  DbContext,
  ErrorContextInterface,
  Event,
  EventContextInterface,
  Identity,
  ProductType,
  ProductTypeElement,
  ReducerEventContextInterface,
  SubscriptionBuilderImpl,
  SubscriptionEventContextInterface,
  SumType,
  SumTypeVariant,
  TableCache,
  TimeDuration,
  Timestamp,
  deepEqual,
} from "@clockworklabs/spacetimedb-sdk";
import { StatBlockDirtyFlagComponent } from "./stat_block_dirty_flag_component_type";
import { EventContext, Reducer, RemoteReducers, RemoteTables } from ".";

/**
 * Table handle for the table `total_stat_block_dirty_flag_components`.
 *
 * Obtain a handle from the [`totalStatBlockDirtyFlagComponents`] property on [`RemoteTables`],
 * like `ctx.db.totalStatBlockDirtyFlagComponents`.
 *
 * Users are encouraged not to explicitly reference this type,
 * but to directly chain method calls,
 * like `ctx.db.totalStatBlockDirtyFlagComponents.on_insert(...)`.
 */
export class TotalStatBlockDirtyFlagComponentsTableHandle {
  tableCache: TableCache<StatBlockDirtyFlagComponent>;

  constructor(tableCache: TableCache<StatBlockDirtyFlagComponent>) {
    this.tableCache = tableCache;
  }

  count(): number {
    return this.tableCache.count();
  }

  iter(): Iterable<StatBlockDirtyFlagComponent> {
    return this.tableCache.iter();
  }
  /**
   * Access to the `entityId` unique index on the table `total_stat_block_dirty_flag_components`,
   * which allows point queries on the field of the same name
   * via the [`TotalStatBlockDirtyFlagComponentsEntityIdUnique.find`] method.
   *
   * Users are encouraged not to explicitly reference this type,
   * but to directly chain method calls,
   * like `ctx.db.totalStatBlockDirtyFlagComponents.entityId().find(...)`.
   *
   * Get a handle on the `entityId` unique index on the table `total_stat_block_dirty_flag_components`.
   */
  entityId = {
    // Find the subscribed row whose `entityId` column value is equal to `col_val`,
    // if such a row is present in the client cache.
    find: (col_val: bigint): StatBlockDirtyFlagComponent | undefined => {
      for (let row of this.tableCache.iter()) {
        if (deepEqual(row.entityId, col_val)) {
          return row;
        }
      }
    },
  };

  onInsert = (cb: (ctx: EventContext, row: StatBlockDirtyFlagComponent) => void) => {
    return this.tableCache.onInsert(cb);
  }

  removeOnInsert = (cb: (ctx: EventContext, row: StatBlockDirtyFlagComponent) => void) => {
    return this.tableCache.removeOnInsert(cb);
  }

  onDelete = (cb: (ctx: EventContext, row: StatBlockDirtyFlagComponent) => void) => {
    return this.tableCache.onDelete(cb);
  }

  removeOnDelete = (cb: (ctx: EventContext, row: StatBlockDirtyFlagComponent) => void) => {
    return this.tableCache.removeOnDelete(cb);
  }

  // Updates are only defined for tables with primary keys.
  onUpdate = (cb: (ctx: EventContext, oldRow: StatBlockDirtyFlagComponent, newRow: StatBlockDirtyFlagComponent) => void) => {
    return this.tableCache.onUpdate(cb);
  }

  removeOnUpdate = (cb: (ctx: EventContext, onRow: StatBlockDirtyFlagComponent, newRow: StatBlockDirtyFlagComponent) => void) => {
    return this.tableCache.removeOnUpdate(cb);
  }}
